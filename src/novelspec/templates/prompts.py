"""Prompt / agent instruction template files."""


def prompt_daily_write() -> str:
    return """# Daily Write — 每日创作指令

> Agent 在运行 /novel:write 时执行的指令。

## 输入读取（按顺序）

1. 读取 novel.yaml（元信息）
2. 读取 world/（世界观框架）
3. 读取 characters/（人物框架）
4. 读取 style/（风格框架）
5. 读取 plot/overview.md 和对应部/outline（剧情大纲）
6. 读取 state/current-status.md（找到下一章）
7. 读取 state/chapter-log.md（了解当前状态）
8. 读取 state/unresolved-threads.md（未解决线索）
9. 读取最近 5 章的 summary.md（章节概要，不要读完整 content.md）

## 创作步骤

1. **理解上下文**：综合以上输入，理解当前故事进展
2. **检查埋点**：查看 foreshadowing.md，判断是否有需要在本章回收的埋点
3. **生成章节正文**：
   - 严格按照 plot/outline 中该章的大纲写作
   - 匹配 style/conventions.md 中的写作约定
   - 参考 style/narrative-samples.md 模仿文风
   - 人物对话符合 speech-patterns.md 中的说话方式
   - 遵守 style/forbidden.md 中的禁忌清单
   - 遵循 rules/quality-standards.md 中的创作质量准则
4. **更新 summary.md**：写完内容后，生成 300-500 字的章节概要
5. **检查新埋点**：如果本章引入了新的伏笔，更新 foreshadowing.md
6. **更新状态**：更新 state/ 下的所有文件
7. **建议审阅**：写完一章后，主动向用户建议运行 `/novel:review ch-NNN` 进行读者视角审阅

## 产出物

```
chapters/part-XX/ch-NNN/
├── summary.md        # 章节概要（含 front matter）
└── content.md        # 章节正文（含 front matter）
```

### content.md 的 front matter 格式

```markdown
---
chapter: NNN
title: 章节标题
part: XX
word_count: 字数
pov: 视角人物
written_at: YYYY-MM-DD
status: draft
key_events:
  - 事件1
new_foreshadowing:
  - FS-XXX: 描述
character_appearances:
  - 人物1
---
```

## 注意

- 不要读旧的 content.md 全文，只读 summary.md
- 如果发现框架层文件有矛盾，暂停并通知用户
- 保持每章字数在 novel.yaml 中每日目标字数的 ±20% 范围内
"""


def prompt_revise_chapter() -> str:
    return """# Revise Chapter — 修改章节指令

> Agent 在运行 /novel:revise 时的指令。

## 输入读取

1. 读取用户指定的章节 content.md 和 summary.md
2. 读取所有框架层文件（world/ + characters/ + plot/ + style/）

## 修改流程

**Step 1 — 分析影响范围**：

判断用户修改请求属于哪一类：

| 类别 | 处理方式 |
|------|---------|
| 🔤 仅润色（措辞、错别字、语句通顺） | 直接修改 content.md，不动其他文件 |
| 📖 调整情节（事件顺序、细节增删） | 修改 content.md + 同步更新 summary.md |
| 👤 影响人物设定 | 修改 content.md + 建议更新 characters/ 中对应文件 |
| 🌍 影响世界观/剧情走向 | 提示用户创建 framework/ 提案（/novel:propose） |

**Step 2 — 执行修改**：

- 修改 content.md 前，将当前版本保存到 revisions/ 目录
- 命名格式：`revisions/v{N}-YYYY-MM-DD.md`
- 修改后同步更新 summary.md（如需要）

**Step 3 — 输出影响报告**：

```
✓ 已修改 ch-NNN/content.md
✓ 旧版本已保存至 revisions/v{N}-YYYY-MM-DD.md
✓ 已更新 summary.md

【影响分析】
✅ 未影响框架层
⚠️ 注意：这个修改可能影响 ch-NNN+1 的剧情走向
```
"""


def prompt_audit_consistency() -> str:
    return """# Audit Consistency — 一致性审计指令

> Agent 在运行 /novel:audit 时的指令。

## 检查项

### 1. 人物一致性
- 扫描所有已写章节的 summary.md
- 检查人物性格、背景、能力是否前后一致
- 检查人物名字拼写是否统一

### 2. 时间线一致性
- 检查所有章节中的时间引用是否矛盾
- 检查 world/timeline.md 中的事件是否与故事内容冲突

### 3. 专有名词一致性
- 检查所有已写章节中的专有名词使用是否与 glossary.md 一致

### 4. 埋点回收检查
- 检查 foreshadowing.md 中状态为"待回收"的埋点
- 如果某埋点的"计划回收章节" ≤ 当前已写章节数，标记为 ⏰ 已过期

### 5. 未登记埋点检测
- 扫描已写章节 summary.md
- 发现可能构成伏笔的内容但未在 foreshadowing.md 中登记 → 建议补充

## 输出格式

```
📋 NovelSpec 一致性审计报告

✅ [领域] — 状态摘要

⚠️ 埋点检查
┌──────┬──────────┬──────────┬────────┐
│ ID   │ 描述     │ 计划回收 │ 状态   │
├──────┼──────────┼──────────┼────────┤
│      │          │          │        │
└──────┴──────────┴──────────┴────────┘

⚡ 建议事项
- 
```
"""


def prompt_worldbuild() -> str:
    return """# Worldbuild — 世界观框架生成指令

> Agent 在运行 /novel:worldbuild 时的指令。

## 工作流

### 用户提供素材后

按依赖关系逐项生成：

```
world/background.md
  → world/timeline.md
  → world/glossary.md
  → characters/profiles/（主要人物）
  → characters/relationships.md
  → characters/speech-patterns.md
  → plot/overview.md（总纲）
  → plot/parts/（各部梗概）
  → plot/foreshadowing.md（初始埋点设计）
  → style/（文风、禁忌、约定）
```

### 生成原则

1. 每次生成一个文件，展示给用户确认后再继续下一个
2. 依赖尚未确认的文件时，基于当前最佳理解先行生成
3. 用户确认后标记该文件为 ✅ 已确认
4. 如果用户想修改已确认的文件，使用 /novel:propose

### 二次调用（已有框架后再次运行）

- 读取现有框架文件
- 展示当前框架结构
- 询问用户想修改哪部分
- 生成修改影响分析
- 用户确认后更新

## 输出风格

- 纯简体中文
- 文件内容使用 Markdown
- 人物卡应包含：外貌、性格、背景故事、动机、成长弧
- 世界观应包含足够的可扩展空间
"""


def prompt_outline() -> str:
    return """# Outline — 章节大纲创作指令

> Agent 在运行 /novel:outline 时的指令。

## 输入

1. 读取 novel.yaml（总章数规划）
2. 读取 plot/overview.md（总纲）
3. 读取 plot/parts/ 中对应部的概梗
4. 读取 characters/（了解出场人物范围）

## 产出

创建 `plot/outlines/part-XX-outline.md`

### 大纲条目格式

```markdown
## 第 N 章（ch-NNN）
**暂定标题**: 
**视角**: 
**核心事件**:
- 
**埋点**: 
- 
**目标字数**: 2000-2500
**出场人物**: 
- 
```

## 注意

- 严格遵循 plot/parts/ 中的故事梗概
- 每章埋下 0-2 个新埋点，定期回收旧埋点
- 各章之间要有节奏感：张弛交替
- 确保人物出场合理
- 大纲完成后，向用户建议运行 `/novel:review outline part-XX` 审阅大纲合理性
"""


def prompt_reader_review() -> str:
    return r"""# Reader Review — 读者视角审阅指令

> Agent 在运行 /novel:review 时的指令。
>
> 核心原则：用一个"正常读者"的眼睛去读作品，找出任何"这里不对"的地方。
> 你不是在找错别字或语法问题（那是润色的事），而是在找逻辑、情感、智商、节奏上的问题。
> 你是一个挑剔但公平的读者——你能理解作者的意图，但更在意最终的阅读体验是否通透。

## 审阅模式

| 模式 | 命令 | 输入 |
|------|------|------|
| 单章审阅 | `/novel:review ch-NNN` | 章节 content.md + summary.md |
| 批量审阅 | `/novel:review recent N` | 最近 N 章的 summary.md |
| 大纲审阅 | `/novel:review outline part-XX` | 对应部的大纲文件 |
| 剧情框架审阅 | `/novel:review plot` | plot/ 下所有文件 |
| 综合审阅 | `/novel:review all` | 所有已写章节 + 框架 |

## 输入读取顺序

1. 读取 novel.yaml（小说类型、目标读者群体）
2. 读取 rules/quality-standards.md（质量准则）
3. 按审阅类型读取相关文件
4. 读取 characters/ 中相关人物的 profiles（单章审阅时需要）
5. 读取 plot/foreshadowing.md（了解待回收埋点）
6. 读取 state/unresolved-threads.md（了解未解决线索）

## 六维审阅体系

### 1. 合理性检查
核心问题：这个故事在这个情境下，会这样发展吗？

检查要点：
- 人物行为是否符合其性格设定、当前处境、核心动机？
- 人际关系反应是否自然？友谊、仇恨、爱情的产生和转变是否有合理铺垫？
- 反派/对手的决策是否有合理的逻辑？是否在正常智商范围内？
- 事件的因果链条是否完整？是否存在"为了剧情需要而强行发生"的情节？
- 力量体系/世界观规则是否被遵守？有无规则崩坏？
- 每个角色掌握的信息量是否与其地位/势力匹配？基于该信息量做出的决策是否合理？

### 2. 智商在线检查
核心问题：这里有没有让人想吐槽"这人是傻子吗"的情节？

检查要点：
- 主角是否在关键决策中表现出与其设定相符的智力水平？
- 是否存在"剧情需要而强行降智"的场景？包括主角降智和反派降智
- 是否存在"信息不对等但角色做出超认知决策"的逻辑漏洞？
- 战略/战术/权谋决策是否合理？是否考虑了基本常识？
- 是否所有角色都在用"差不多聪明"的方式做事？还是有人被强行当工具人？

### 3. 故事线一致性检查
核心问题：这段内容放在整本书里，合适吗？

检查要点：
- 本章/本大纲是否符合 plot/ 中设定的方向？
- 是否与之前章节存在矛盾（人物信息、时间线、事件经过）？
- 节奏是否符合当前故事阶段：高潮期 → 铺垫期 → 过渡期？
- 信息释放速度是否合适：太快则失去神秘感，太慢则让读者烦躁
- 支线是否在为主线服务？是否有跑题倾向？
- 如果是框架级审阅：故事的整体结构是否有明显的头重脚轻或中间塌陷？

### 4. 情感共鸣检查
核心问题：读者会在这里被触动吗？还是会觉得"哦，所以呢"？

检查要点：
- 情感场景的铺垫是否足够？还是突然就煽情了？
- 是否有"作者想让我感动但我完全没感觉"的强行煽情？
- 情感反应的烈度是否匹配事件的重要性？
- 读者的代入感是否被破坏了？（如主角突然做出不符合共情的决定）
- 角色之间的情感互动是否立体？还是只有主角一人在表演？
- 虐点之前有无足够的甜蜜/温暖作为反差铺垫？

### 5. 阅读体验检查
核心问题：读者读这段的时候，会是什么感受？

检查要点：
- 本章结尾是否有钩子？读完是否有"想看下一章"的冲动？
- 是否有过长的段落/对话/描述，会让读者想要快速跳过？
- 信息密度是否适中：一章塞了太多信息 vs 一章什么都没发生
- 是否有不必要的设定说明破坏了叙事流畅性？
- 句式和词汇是否单调？读起来会不会像"报告"而不是"故事"？
- 开篇是否足够抓人？还是需要"忍过前几段"？

### 6. 对话与行为可信度检查
核心问题：这个人在这个场景下，会这么说/这么做吗？

检查要点：
- 对话是否符合角色的身份、阶层、教育背景、性格？
- 角色在危机/情绪激动/秘密场合的反应是否真实？
- 沉默和克制的表述是否恰当？（不是所有情感都需要用台词表达）
- 对话是否在推动情节的同时也在塑造人物？还是纯信息传递？
- 是否存在"所有角色说话都一样"的问题？
- 古风小说：古风感是否自然？是否有现代词汇乱入？

## 输出格式

### 审阅报告（单章/批量模式）

```markdown
# 📖 读者视角审阅报告

## 基本信息
- **审阅对象**: ch-NNN「章节标题」
- **审阅模式**: 单章审阅
- **审阅时间**: YYYY-MM-DD
- **总体评分**: ★★★★☆ (4/5)
- **一句话点评**: （用一句话概括本章给读者的感受）

---

### 🔴 严重问题（必须修复）

这些问题会直接破坏阅读体验，强烈建议优先处理。

| # | 维度 | 位置 | 问题描述 | 建议修复方案 |
|---|------|------|---------|------------|
| 1 | 合理性 | 第3段 | 主角明知敌人在暗处却独自赴约，不符合其"谨慎"人设 | 增加被迫赴约的合理理由（如亲人被挟持），或改为带暗卫同行但不暴露 |

### 🟡 建议优化

这些问题不影响主线逻辑，但优化后阅读体验更好。

| # | 维度 | 位置 | 问题描述 | 建议修复方案 |
|---|------|------|---------|------------|
| 1 | 阅读体验 | 第5段 | 场景描写略显冗长，节奏偏慢 | 精简为3-4句，聚焦核心意象 |

### ⚪ 可参考意见

- 这可能是个人偏好差异，但值得注意的细节
- ...

### ✅ 本次审阅小结

**亮点**:
- 本章的____处理得当，特别是...

**主要风险**:
- 章节结尾缺少足够的悬念钩子，建议增加...

**综合建议**:
- 建议优先修复 2 个严重问题，再修改 3 个优化项

### 🔄 修改优先级

| 优先级 | 问题编号 | 原因 |
|-------|---------|------|
| P0 | #1 | 直接影响读者理解 |
| P1 | #2, #3 | 影响阅读体验但不致命 |
| P2 | #4 | 可修可不修 |

### 📁 审阅记录

```
chapters/part-XX/ch-NNN/reviews/v1-YYYY-MM-DD.md
```
```

### 审阅报告（大纲/框架模式）

```markdown
# 📖 读者视角审阅报告（框架级）

## 基本信息
- **审阅对象**: 第XX部大纲 / 全文剧情框架
- **审阅模式**: 大纲审阅 / 剧情框架审阅

---

### 🔴 结构性风险

| # | 类型 | 位置 | 风险描述 | 建议调整 |
|---|------|------|---------|---------|
| 1 | 节奏失衡 | 第3-5章 | 连续三章都是高密度剧情冲突，读者会疲劳 | 在第3章和第4章之间插入一章缓冲（日常/情感/解谜） |

### 🟡 可优化项

| # | 类型 | 位置 | 建议 | 
|---|------|------|------|

### ✅ 总体评价
- 结构强度：...
- 读者吸引力：...
- 主要风险：...

### 🔄 建议调整优先级
```

## 审阅后流程

### 审阅驱动修改

审阅报告生成后，建议用户按优先级逐条修改：

1. 用户选择要修改的问题
2. 运行 `/novel:revise ch-NNN 问题描述` 进行定向修改
3. 修改完成后，可再次运行 `/novel:review ch-NNN` 确认问题已解决
4. 将审阅报告存档到 `reviews/` 目录

### 审阅文件存储格式

```markdown
chapters/part-XX/ch-NNN/reviews/
├── v1-YYYY-MM-DD.md    # 第一次审阅
├── v2-YYYY-MM-DD.md    # 修改后二次审阅
└── .gitkeep
```

## 核心原则

1. **不吹毛求疵** — 只找"正常读者会觉得不对劲"的点，不抠可忽略的细节
2. **有据可依** — 每个问题都需要说明"为什么这不对"，而不是"我觉得不好"
3. **给出方案** — 每个问题都要附带修复建议，不能只提问题不给方案
4. **严格分级** — 🔴=必须修 🟡=建议修 ⚪=可参考（不可混用）
5. **区分类型** — 严肃推理、历史正剧、玄幻爽文、言情甜文的标准不同，根据 novel.yaml 中的类型调整审阅尺度
6. **审阅不代替创作** — 审阅指出问题，具体的修复方案由用户判断是否采纳
"""


def prompt_propose_framework() -> str:
    return """# Propose Framework Change — 框架变更提案指令

> Agent 在运行 /novel:propose 时的指令。

## 工作流

1. 读取当前框架文件（world/ + characters/ + plot/ + style/）
2. 读取已写章节的 summary.md（评估影响范围）
3. 在 framework/ 下创建提案目录
4. 生成 proposal.md

### proposal.md 模板

```markdown
# 提案：〈标题〉

## 为什么改
（为什么需要这个变更）

## 改什么
- （列出要修改的具体文件）

## 影响范围
- 框架文件：（哪些框架文件受影响）
- 已写章节：（哪些章节可能需要调整）
- 后续大纲：（哪些后续章节大纲需要调整）

## 备选方案
A. （方案 A）
B. （方案 B）
```

## 原则

- 只生成提案，不执行修改
- 清晰地标注影响范围，让用户判断是否值得改
- 提供至少 2 个备选方案（如果有余地的线）
"""
